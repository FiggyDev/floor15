# FLOOR 15 — Official Contract Addresses

## STATUS: NOT DEPLOYED

**There is no FLOOR 15 token. There is no contract address. Nothing is for sale.**

This file exists BEFORE any token so that there is exactly one source of truth if that ever
changes. Bookmark this file, not a tweet.

## The only rules that matter

1. **The only official addresses are the ones written in THIS file, in THIS repo,
   on the default branch.** No other source is official. Not our own social accounts,
   not screenshots of this file, not "team members" in DMs.
2. An address appears here only after ALL of the following:
   - contract deployed and **verified** on the network's canonical explorer
   - legal/counsel review completed
   - committed to this file by a repo maintainer
   - announced in a signed release tagged in this repo
3. **Never trust:** replies, DMs, Telegram/Discord admins, screenshots, QR codes,
   "urgent migration" posts, lookalike repos, or anyone rushing you. Urgency is the tell.
4. If this section still says NOT DEPLOYED, every address you have seen is a scam.
   Report it, don't send to it.
5. Deletion of this policy, or an address appearing here without the fields below filled,
   is itself a red flag — treat the repo as compromised and wait for a signed release.

## Future record (empty until real — all fields required before any address is official)

| Field | Value |
|---|---|
| Network | — |
| Contract address | — |
| Deploy transaction | — |
| Explorer verification link | — |
| Deploy date | — |
| Committed by (maintainer) | — |
| Signed release tag | — |
| Counsel review | — |

*Linda Legal reviewed this file and, for once, approved it on the first pass.*
