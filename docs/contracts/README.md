# contracts/

**Placeholder only. Nothing is deployed. There is no token.**
This folder holds the address policy (`ADDRESSES.md`) and — only after deploy + verification +
counsel approval, if ever — the official record. No contract source lives here today, on purpose:
an empty folder can't be misread as a soft launch.
